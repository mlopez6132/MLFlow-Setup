# MLFlow-Setup
This repository demonstrates how to use MLflow for remote tracking of machine learning experiments.

## Features
- Logs parameters, metrics, and models to a remote MLflow server.
- Automates experiment runs using GitHub Actions.

## Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
